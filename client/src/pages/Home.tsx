import { ArrowRight, Clapperboard, Film, FolderOpen, PlusCircle, Sparkles } from "lucide-react";
import { useLocation } from "wouter";
import { MemberHeader } from "@/components/MemberHeader";
import { ProMovieGuard, useProMovieMember } from "@/components/ProMovieGuard";
import { trpc } from "@/lib/trpc";

function HomeView() {
  const [, setLocation] = useLocation(); const { token, data: member } = useProMovieMember(); const catalog = trpc.promovie.catalog.useQuery({ token }, { enabled: Boolean(token) });
  if (!member) return null;
  const categories = catalog.data?.categories ?? [];
  return <div className="min-h-screen bg-[#0A0A0A] text-white"><MemberHeader member={member} /><main><section className="relative isolate overflow-hidden"><div className="absolute inset-0 bg-[radial-gradient(circle_at_76%_22%,rgba(229,9,20,.42),transparent_30%),radial-gradient(circle_at_55%_80%,rgba(113,20,25,.22),transparent_36%),linear-gradient(135deg,#1b080a,#080808_58%,#220b0d)] opacity-90" /><div className="absolute inset-0 bg-gradient-to-r from-[#0A0A0A] via-[#0A0A0A]/85 to-[#0A0A0A]/25" /><div className="container relative flex min-h-[440px] items-center py-20"><div className="max-w-2xl"><span className="inline-flex items-center gap-2 rounded-full border border-red-500/25 bg-red-500/10 px-3 py-1.5 text-xs font-bold uppercase tracking-[.16em] text-red-300"><Sparkles className="h-3.5 w-3.5" />Private streaming</span><h1 className="mt-6 font-display text-6xl leading-[.88] tracking-[-.06em] sm:text-7xl">Your next watch<br /><span className="text-[#E50914]">starts here.</span></h1><p className="mt-6 max-w-lg text-base leading-7 text-zinc-300">Browse your own categories, open a movie folder, and select a real episode to watch in Bro Player.</p><div className="mt-9 flex flex-wrap gap-3"><button onClick={() => setLocation("/profile")} className="primary-action inline-flex items-center gap-2">View my wallet <ArrowRight className="h-4 w-4" /></button>{member.role === "admin" && <button onClick={() => setLocation("/admin")} className="secondary-action inline-flex items-center gap-2"><PlusCircle className="h-4 w-4" />Open Admin Panel</button>}</div></div></div></section><section className="container py-14"><div className="mb-7 flex items-end justify-between"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-[#E50914]">Library folders</p><h2 className="mt-2 font-display text-4xl tracking-[-.05em]">Browse by category</h2></div><span className="hidden text-sm text-zinc-500 sm:block">Only administrator-published content appears here.</span></div>{categories.length === 0 ? <div className="relative overflow-hidden rounded-[1.8rem] border border-white/[.08] bg-gradient-to-br from-zinc-900/90 to-[#090909] p-7 sm:p-12"><div aria-hidden="true" className="absolute -right-10 -top-16 h-64 w-64 rounded-full bg-[radial-gradient(circle,rgba(229,9,20,.28),transparent_66%)] blur-2xl sm:right-4 sm:top-0" /><div className="relative max-w-xl"><div className="grid h-12 w-12 place-items-center rounded-2xl bg-[#E50914]/12 text-[#ff4b56]"><FolderOpen className="h-6 w-6" /></div><h3 className="mt-6 font-display text-4xl tracking-[-.05em]">The collection is empty.</h3><p className="mt-3 text-sm leading-6 text-zinc-400">There are no categories or movies yet. The administrator can add a category cover, create movie folders, and then publish real episodes.</p>{member.role === "admin" && <button onClick={() => setLocation("/admin")} className="secondary-action mt-7 inline-flex items-center gap-2"><Clapperboard className="h-4 w-4" />Create a category</button>}</div></div> : <div className="grid gap-5 sm:grid-cols-2 xl:grid-cols-3">{categories.map(category => <button key={category.id} onClick={() => setLocation(`/category/${category.id}`)} className="group relative aspect-[16/9] overflow-hidden rounded-[1.5rem] border border-white/[.08] bg-zinc-900 text-left transition duration-200 hover:-translate-y-1 hover:border-red-500/45">{category.coverUrl ? <img src={category.coverUrl} alt={`${category.name} cover`} className="h-full w-full object-cover transition duration-500 group-hover:scale-105" /> : <div className="h-full w-full bg-[radial-gradient(circle_at_72%_22%,rgba(229,9,20,.32),transparent_38%),linear-gradient(135deg,#262626,#090909)]" />}<div className="absolute inset-0 bg-gradient-to-t from-black via-black/30 to-transparent" /><div className="absolute bottom-0 left-0 right-0 p-5"><p className="text-xs font-bold uppercase tracking-[.18em] text-red-300">Open folder</p><h3 className="mt-1 font-display text-4xl tracking-[-.05em]">{category.name}</h3></div></button>)}</div>}</section></main></div>;
}
export default function Home() { return <ProMovieGuard><HomeView /></ProMovieGuard>; }
