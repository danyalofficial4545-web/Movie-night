import { CalendarDays, CircleUserRound, Download, ExternalLink, Film, Gift, History, Landmark, LoaderCircle, Mail, Phone, WalletCards } from "lucide-react";
import { useLocation } from "wouter";
import { MemberHeader } from "@/components/MemberHeader";
import { ProMovieGuard, useProMovieMember } from "@/components/ProMovieGuard";
import { trpc } from "@/lib/trpc";

const publicUrl = "https://earnpackpro-3nzfwkdg.manus.space";
function ProfileView() { const [, setLocation] = useLocation(); const { token, data: member } = useProMovieMember(); const profile = trpc.promovie.profile.useQuery({ token }, { enabled: Boolean(token) }); if (!member || profile.isLoading) return <div className="grid min-h-screen place-items-center bg-[#0A0A0A]"><LoaderCircle className="h-7 w-7 animate-spin text-[#E50914]" /></div>; const data = profile.data; if (!data) return null; const details = data.profile; const referralLink = `${publicUrl}/ref/${details.id}`; return <div className="min-h-screen bg-[#0A0A0A] text-white"><MemberHeader member={member} /><main className="container py-10 sm:py-14"><div className="grid gap-6 lg:grid-cols-[1.05fr_.95fr]"><section className="rounded-[1.7rem] border border-white/[.09] bg-zinc-950 p-6 sm:p-8"><div className="flex gap-5"><div className="grid h-20 w-20 shrink-0 place-items-center rounded-3xl bg-zinc-800 text-zinc-300"><CircleUserRound className="h-10 w-10" /></div><div><p className="text-xs font-bold uppercase tracking-[.18em] text-[#E50914]">Member profile</p><h1 className="mt-2 font-display text-5xl tracking-[-.06em]">{details.name || "ProMovie member"}</h1><p className="mt-2 text-sm text-zinc-500">Joined {new Date(details.createdAt).toLocaleDateString()}</p></div></div><div className="mt-8 grid gap-3 border-t border-white/[.07] pt-6 text-sm text-zinc-400"><p className="flex items-center gap-3"><Mail className="h-4 w-4" />{details.email}</p><p className="flex items-center gap-3"><Phone className="h-4 w-4" />{details.mobile}</p><p className="flex items-center gap-3"><CalendarDays className="h-4 w-4" />Member since {new Date(details.createdAt).toLocaleDateString()}</p></div></section><section className="relative overflow-hidden rounded-[1.7rem] border border-[#FFC107]/20 bg-gradient-to-br from-[#2c2102] via-[#171102] to-zinc-950 p-6 sm:p-8"><WalletCards className="h-7 w-7 text-[#FFC107]" /><p className="mt-8 text-sm font-bold uppercase tracking-[.18em] text-[#ffda6b]">Wallet balance</p><p className="mt-2 font-display text-7xl tracking-[-.07em] text-[#FFC107]">{details.coinBalance.toLocaleString()}</p><p className="mt-1 text-sm text-[#f4d77a]/70">1000 coins = PKR 5 · 50 coins per active minute.</p><button onClick={() => setLocation("/withdraw")} className="primary-action mt-7 inline-flex items-center gap-2"><Landmark className="h-4 w-4" />Request withdrawal</button></section></div><section className="mt-7 rounded-[1.7rem] border border-white/[.08] bg-zinc-950 p-6 sm:p-8"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-red-500/12 text-red-300"><Gift className="h-5 w-5" /></span><div><p className="text-xs font-bold uppercase tracking-[.15em] text-[#E50914]">Referral rewards</p><h2 className="font-display text-3xl">Invite a member</h2></div></div><div className="mt-6 grid gap-4 lg:grid-cols-[1fr_auto_auto]"><div className="rounded-xl border border-white/10 bg-black/30 px-4 py-3 text-sm text-zinc-300 break-all">{referralLink}</div><button onClick={() => navigator.clipboard.writeText(referralLink)} className="secondary-action">Copy link</button><div className="rounded-xl border border-[#FFC107]/15 bg-[#FFC107]/[.06] px-4 py-3 text-sm"><b className="text-[#FFC107]">{data.referralCount}</b> referrals · <b className="text-[#FFC107]">{details.referralCoinsEarned}</b> coins earned</div></div><p className="mt-4 text-xs text-zinc-500">When a new user creates an account through your link, both accounts receive 200 coins.</p></section><section className="mt-10 grid gap-6 xl:grid-cols-2"><HistoryList title="Earnings history" icon={<History className="h-5 w-5" />} rows={data.transactions.map(item => ({ title: item.note || item.activity, detail: `${item.coinsDelta > 0 ? "+" : ""}${item.coinsDelta} coins · ${new Date(item.createdAt).toLocaleString()}` }))} empty="No coin activity yet." /><HistoryList title="Watch history" icon={<Film className="h-5 w-5" />} rows={data.watches.map(item => ({ title: item.episodeLabel || "Watched video", detail: `${Math.floor(item.watchedSeconds / 60)} min · Earned ${item.coinsEarned} coins · ${new Date(item.lastPlayedAt).toLocaleString()}` }))} empty="No watched episodes yet." /><HistoryList title="My downloads" icon={<Download className="h-5 w-5" />} rows={data.downloads.map(item => ({ title: `Movie #${item.movieId}`, detail: new Date(item.createdAt).toLocaleString() }))} empty="No downloads yet." /><HistoryList title="Withdrawal requests" icon={<Landmark className="h-5 w-5" />} rows={data.withdrawals.map(item => ({ title: `${item.method} · PKR ${item.pkrAmount}`, detail: `${item.coins} coins · ${item.status} · ${new Date(item.createdAt).toLocaleString()}` }))} empty="No withdrawal requests yet." /></section></main></div>; }
function HistoryList({ title, icon, rows, empty }: { title: string; icon: React.ReactNode; rows: { title: string; detail: string }[]; empty: string }) { return <section className="rounded-[1.5rem] border border-white/[.08] bg-zinc-950 p-6"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-white/[.05] text-zinc-300">{icon}</span><h2 className="font-display text-3xl tracking-[-.05em]">{title}</h2></div>{rows.length === 0 ? <p className="mt-6 border-t border-dashed border-white/10 pt-6 text-sm text-zinc-500">{empty}</p> : <div className="mt-6 divide-y divide-white/[.06] border-t border-white/[.08]">{rows.map((row, index) => <div className="py-3" key={`${row.title}-${index}`}><p className="text-sm font-bold text-zinc-200">{row.title}</p><p className="mt-1 text-xs text-zinc-500">{row.detail}</p></div>)}</div>}</section>; }
export default function Profile() { return <ProMovieGuard><ProfileView /></ProMovieGuard>; }
